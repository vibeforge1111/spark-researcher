# Prompt Stack Invocation

This chip was bootstrapped from the parent workspace's `docs/master_chip_v2` stack on `2026-03-13`.

## Prompt Order

1. `docs/master_chip_v2/MASTER_CHIP_ARCHITECT_PROMPT_V2.md`
2. `docs/master_chip_v2/MASTER_CHIP_OPERATOR_PROMPT.md`
3. `docs/master_chip_v2/MASTER_CHIP_IMPLEMENTATION_PROMPT.md`
4. `docs/master_chip_v2/MASTER_CHIP_TESTING_PROMPT.md`
5. `docs/master_chip_v2/MASTER_CHIP_REVIEW_PROMPT.md`

## Filled Domain Values

- `DOMAIN_NAME = web-design`
- `CHIP_NAME = domain-chip-web-designer`
- `PACKAGE_NAME = domain_chip_web_designer`
- `CHIP_PATH = C:\Users\USER\Desktop\spark-researcher\domain-chip-web-designer`
- `PRIMARY_METRIC = web_design_score`

## Architect Brief

Design a Spark domain chip that becomes excellent at website, dashboard, and product UI design by learning from award ecosystems without turning into a style-copying machine.

The chip must:

- extract reusable patterns from award sites and inspiration galleries
- separate visual spectacle from actual UI/UX quality
- encode accessibility, information hierarchy, task flow, proof placement, and conversion logic as first-class constraints
- improve recursively through benchmark evidence instead of taste-only narration
- stay small, portable, and private-repo-ready

## Operator Brief

Build a chip that can answer:

- what makes a website feel world class
- when an award-style direction helps versus hurts usability
- which interaction patterns are high craft but still safe
- which website systems should be promoted into doctrine for landing pages, dashboards, and portfolios

## Implementation Brief

The repo should implement four hooks:

- `evaluate`: score a candidate design system on award signal, UX, interaction craft, conversion clarity, and accessibility safety
- `suggest`: propose coherent untested design systems rather than random style combinations
- `packets`: emit reusable design lessons and boundaries
- `watchtower`: render the active domain state into pages

## Testing Brief

The first validation layer should prove:

- the manifest is schema-valid
- the chip executes all four hooks
- benchmark outputs are deterministic
- accessible candidates can outperform flashy but unsafe candidates
- baseline output stays clearly non-promotional

## Review Gate

Do not approve mutations that:

- increase spectacle while reducing accessibility safety
- optimize for inspiration-gallery aesthetics without task clarity
- promote dashboards that are visually dense but cognitively weak
- confuse benchmark elegance with real-world conversion truth
